spring:
  application:
    name: aggregator
  config:
    import: ${SPRING_CONFIG_IMPORT:configserver:http://localhost:8888}
  cloud:
    config:
      fail-fast: true
      retry:
        useRandomPolicy: true
        max-interval: 6000